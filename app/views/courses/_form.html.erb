<h2>Add a new course</h2>
<%= form_for @course do |f| %>
<p>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <p>
    Daytime? <%= f.check_box(:daytime) %>
        <p>
          <%= f.submit 'Add course!' %>
          <% end %>
